import { Injectable } from '@angular/core';
/*import { FormGroup, FormControl, Validators} from "@angular/forms";*/

@Injectable({
  providedIn: 'root'
})
export class AdminService {

  constructor() { }

  /*
  form: FormGroup = new FormGroup({
    $key: new FormControl(null),
    titulo: new FormControl('', [Validators.required, Validators.maxLength(150)]),
    questionarios: new FormControl(''),
    periodo: new FormControl('', [Validators.required, Validators.maxLength(1)]),
    ativo: new FormControl(true),
    terminado: new FormControl(false),
    data_ativacao: new FormControl(''),
    data_limite: new FormControl(''),
    observacao: new FormControl(''),
    publico_alvo: new FormControl(''),    
    participantes: new FormControl('')
  }); */
}
